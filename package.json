{
  "name": "arancini",
  "version": "1.0.0",
  "private": false,
  "scripts": {
    "build": "npm run build:all",
    "test": "npm run test:all",
    "lint": "npm run lint:all",
    "change": "changeset",
    "bump": "changeset version && npm install",
    "release": "npm run build && npm test && changeset publish",
    "build:all": "echo 'build scripts for individual packages can be added here'",
    "test:all": "echo 'test scripts for individual packages can be added here'",
    "lint:all": "echo 'lint scripts for individual packages can be added here'"
  },
  "devDependencies": {
    "@changesets/cli": "^2.26.1",
    "concurrently": "^8.2.1"
  }
}